# Android install scripts for each product

This directory contains OS extensions that are application specific.
e.g. These patches include features for kiosk mode support of apps and locking out other OS features for example.

# TODO
- consider making all these scripts one generic script and it becoming parameterised more specifically in platform/config.mk or the more generic PLATFOMR_SPECIFC scripts used in envtree for all os types.