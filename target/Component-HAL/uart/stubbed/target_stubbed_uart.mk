EHS_TARGET_HAL_UART_STUBBED_PATH=$(EHS_TARGET_COMPONENT_HAL_PATH)/uart/stubbed
VPATH +=: $(EHS_TARGET_HAL_UART_STUBBED_PATH)
INC_DIRS += $(EHS_TARGET_HAL_UART_STUBBED_PATH)

ifeq ($(EHS_UART_SUPPORT),yes)
else
OBJECTS += $(EHS_TARGET_HAL_UART_STUBBED_PATH)/target_uart.${OBJ}
endif