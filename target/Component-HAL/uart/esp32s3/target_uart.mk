# ESP32-S3 IDF UART HAL.
# The IDF driver headers (driver/uart.h, freertos/*.h, etc.) are already in
# the include path via the esp32 toolchain and target.mk setup — no extra
# INC_DIRS are needed here.
# VPATH, INC_DIRS, and OBJECTS are set by uart_common.mk.
