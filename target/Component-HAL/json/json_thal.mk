EHS_TARGET_JSON_PATH=$(EHS_TARGET_COMPONENT_HAL_PATH)/json
VPATH +=: $(EHS_TARGET_JSON_PATH)
INC_DIRS += $(EHS_TARGET_JSON_PATH)

# EHS_TARGET_LORAWAN_SRCS:=$(wildcard lorawan*.c)
# $(error srcs are $(EHS_TARGET_LORAWAN_SRCS))
# 
# #EHS_TARGET_LORAWAN_SRCS +=
# 
# OBJECTS += $(EHS_TARGET_LORAWAN_SRCS:.c=.o)

OBJECTS+= jsmn.$(OBJ)
OBJECTS += jsmn_utils.$(OBJ)

