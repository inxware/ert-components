EHS_TARGET_WIFI_PATH=$(EHS_TARGET_COMPONENT_HAL_PATH)/wifi
VPATH +=: $(EHS_TARGET_WIFI_PATH)
INC_DIRS += $(EHS_TARGET_WIFI_PATH)

# EHS_TARGET_LORAWAN_SRCS:=$(wildcard lorawan*.c)
# $(error srcs are $(EHS_TARGET_LORAWAN_SRCS))
# 
# #EHS_TARGET_LORAWAN_SRCS +=
# 
# OBJECTS += $(EHS_TARGET_LORAWAN_SRCS:.c=.o)

# OBJECTS += xxxxxx.$(OBJ)

DEFS += EHS_NETWORK_WIFI_SUPPORT