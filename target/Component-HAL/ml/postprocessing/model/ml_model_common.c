/* DEPRECATED — ml_model_common.c
 *
 * The inference engine dispatch layer has been moved to:
 *   target/Component-HAL/ml/engine/ml_inf_engine.c
 *
 * This file is no longer compiled (removed from ml_model.mk).
 * It is kept in the repository temporarily to make the rename visible in
 * git history.  Delete it once no branch references it.
 */
