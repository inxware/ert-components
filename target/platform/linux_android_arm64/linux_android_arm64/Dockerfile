FROM ubuntu:22.04
#todo2022 this docker file needs to be updated with the SDK installer as for the 32bit android arm DOckerfile. (In fact do we need a different docker file at all?)
RUN apt-get update && \
    apt-get install -y build-essential git git-lfs openjdk-8-jdk 
